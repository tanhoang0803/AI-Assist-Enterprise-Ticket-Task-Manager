{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @repo/types build && pnpm --filter @repo/utils build && cd apps/web && next build",
  "outputDirectory": ".next"
}
